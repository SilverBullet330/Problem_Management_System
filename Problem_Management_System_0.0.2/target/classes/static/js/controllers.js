function HomeController($scope, $route) {
	$scope.$route = $route;
}

function ProblemController($scope, $route) {
	$scope.$route = $route;
}

function CourseController($scope, $route) {
	$scope.$route = $route;
}

function CommentController($scope, $route) {
	$scope.$route = $route;
}

function TrolleyController($scope, $route) {
	$scope.$route = $route;
}

function UserinfoController($scope, $route) {
	$scope.$route = $route;
}

function AdminController($scope, $route) {
	$scope.$route = $route;
}

function AboutController($scope, $route) {
	$scope.$route = $route;
}